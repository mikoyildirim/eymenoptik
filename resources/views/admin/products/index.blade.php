@extends('adminlte::page')
@section('title','Ürünler')
@section('content_header')<div class="d-flex justify-content-between"><h1>Ürünler</h1><a href="{{ route('admin.products.create') }}" class="btn btn-primary">Yeni Ürün</a></div>@endsection
@section('content')@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="card-body table-responsive"><table class="table table-bordered"><tr><th>Görsel</th><th>Ürün</th><th>Kategori</th><th>Fiyat</th><th>Stok</th><th></th></tr>@foreach($products as $product)<tr><td><img src="{{ $product->image_url }}" style="width:70px;height:50px;object-fit:cover;border-radius:8px"></td><td>{{ $product->name }}</td><td>{{ $product->category->name ?? '-' }}</td><td>{{ number_format($product->price,2) }} ₺</td><td>{{ $product->stock }}</td><td><a href="{{ route('admin.products.edit',$product) }}" class="btn btn-sm btn-warning">Düzenle</a><form action="{{ route('admin.products.destroy',$product) }}" method="POST" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Sil</button></form></td></tr>@endforeach</table>{{ $products->links() }}</div></div>@endsection
